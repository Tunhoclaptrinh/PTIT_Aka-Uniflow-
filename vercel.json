{
  "buildCommand": "npm install --include=dev && npm run build:packages && npm run build --workspace=apps/web",
  "outputDirectory": "apps/web/dist",
  "installCommand": "echo 'skip'",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
